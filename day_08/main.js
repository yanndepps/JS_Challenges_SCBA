/*
  * First Digit
  * Find the leftmost digit that occurs in a given string
*/

function firstDigit(str) {
  // code
}

const str = 'var_1_Int2';
const result = firstDigit(str);
console.log("result: ", result);
